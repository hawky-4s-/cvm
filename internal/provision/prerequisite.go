package provision

func checkPrerequisites() {
	// check if following commands are accessible and have the correct version
	// - docker
	// - docker-compose
	// otherwise return hint with installation requirements
}
